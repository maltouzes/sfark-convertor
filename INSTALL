Installing Sfark-convertor
=================

Still in beta

Requirements:

The following dependencies are required:

    Python 2.7.6
    Kivy 1.9.0 (should work on Kivy 1.7.2 too, but not tested)
    https://github.com/raboof/sfArkLib
    https://github.com/raboof/sfarkxtc (need root installation: sudo make install

Installation:
 - On Ubuntu (15.10 and above) and Debian (8 and above) all the dependencies should be in the official repositories
 sudo apt-get install sfarkxtc python-kivy

Usage:

Run main.py
